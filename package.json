{
  "name": "aicode-academy",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun browser && astro dev",
    "browser": "bun start brave -incognito  http://localhost:4321",
    "start": "astro dev",
    "build": "astro build",
    "serve": "npm run build && serve dist",
    "preview": "astro preview",
    "astro": "astro",
    "update": "npm update --save && npm outdated",
    "master": "git checkout master && git merge blog",
    "deploy": "git checkout production && git merge master && git push origin production && git checkout master"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.3.0",
    "@fontsource/fira-mono": "^5.2.5",
    "@fontsource/tomorrow": "^5.2.5",
    "@fontsource/major-mono-display": "^5.2.5",
    "@picocss/pico": "^2.1.1",
    "astro": "^5.5.5"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
