{
  "name": "aicode-academy",
  "description": "Web site y blog de Aicode Academy",
  "author": {
    "name": "Alberto Basalo",
    "email": "alberto@aicode.academy"
  },
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "serve": "npm run build && serve dist",
    "preview": "astro preview",
    "astro": "astro",
    "update": "npm update --save && npm outdated",
    "master": "git checkout master && git merge blog",
    "deploy": "git checkout production && git merge master && git push origin production && git checkout master"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.3.0",
    "@picocss/pico": "^2.1.1",
    "astro": "^5.5.5"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
